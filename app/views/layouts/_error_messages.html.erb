<% if object.errors.any? %>
  <div class="error_explanation">
    <%= object.errors.count %> errors prohibited this book from being saved:
    <ul>
      <%= object.errors.full_messages.each do |message|%>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>